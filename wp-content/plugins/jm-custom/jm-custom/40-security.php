<?php
if (!defined('ABSPATH')) exit;

/**
 * JM Security
 * - Keep conservative defaults; avoid breaking integrations.
 */

// Example (optional): disable XML-RPC.
// add_filter('xmlrpc_enabled', '__return_false');
